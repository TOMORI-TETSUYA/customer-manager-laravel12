# SECURITY.md — Patron Hub セキュリティ方針

## 1. 公開領域の最小化

- ドキュメントルートは `customer-management/` のみ。公開される PHP は `php/index.php` の1ファイルだけです。
- Laravel 本体(`customer-management_app/`)・`.env`・`storage/` は Web から到達不能な位置に配置しています。
- Nginx は `index.php` 以外の PHP 実行を許可しません。

### 検索エンジンへの非掲載

顧客情報を扱うため、検索結果に URL すら出さない方針です。3段構えで防いでいます。

| 手段 | 場所 | 役割 |
| --- | --- | --- |
| `robots.txt` | `customer-management/robots.txt` | 巡回そのものを断る。主要検索エンジン・生成AI・アーカイブ計30種を明示的に拒否 |
| `X-Robots-Tag` ヘッダー | `docker/nginx/default.conf` | robots.txt を無視されても登録させない。画像など meta を書けないファイルにも効く |
| `<meta name="robots">` | `resources/views/layouts/*.blade.php` | 同じ指示を HTML 側にも記載 |

指定値は `noindex, nofollow, noarchive, nosnippet, noimageindex, notranslate` です。

> **Nginx を編集するときの注意**: `add_header` は「そのブロックに1つでも `add_header` があると上位ブロックの指定を引き継がない」仕様です。
> このため静的ファイル用の `location` ブロックにも全ヘッダーを書き直しています。片方だけ変更するとヘッダーが欠落します。

なお、ログイン画面以外はすべて認証が必要なため、未認証のクローラーが到達できるのはログイン画面のみです。

## 2. 認証・セッション

- パスワードは **Argon2id** でハッシュ化(平文・可逆保存は行いません)。
- パスワードポリシー: **12文字以上**。初期パスワードは初回ログイン時に変更を強制(`password.changed` ミドルウェア)。
- ログイン試行制限: **同一 ログインID+IP で 3回/60分**(RateLimiter)。超過時は残り時間のみ提示し、ID の存在有無は伝えません。
- 総当たり対策として応答を意図的に遅らせます。**失敗時10秒 / ブロック中120秒**。
  値は `.env` の `LOGIN_MAX_ATTEMPTS` / `LOGIN_DECAY_MINUTES` / `LOGIN_FAILED_DELAY` / `LOGIN_BLOCKED_DELAY` で調整します。

  > 遅延中は PHP-FPM のプロセスを1つ占有します。同時に遅延できる数は `docker/php/www.conf` の `pm.max_children`(現在40)が上限で、これを超えると通常の利用者まで待たされます。
  > 遅延秒数を延ばす場合は `php.ini` の `max_execution_time`、`www.conf` の `request_terminate_timeout`、Nginx の `fastcgi_read_timeout`(いずれも現在180秒)も併せて延ばしてください。
- ログイン失敗時は「ログインIDまたはパスワードが正しくありません。」の **統一文言** とし、ID の存在有無を推測させません。
- ログイン成功時に **セッションIDを再生成**、ログアウト時に無効化+トークン再生成。

### セッションの有効期限

| 種類 | 期限 | 実装 |
| --- | --- | --- |
| 無操作での失効 | **30分** | `SESSION_LIFETIME`(操作するたびに延長) |
| ログインからの上限 | **3日** | `LOGIN_MAX_DAYS` / `EnsureLoginNotExpired` ミドルウェア |
| 「ログイン状態を保持する」のCookie | **3日** | `LoginController` で `setRememberDuration()` |

「ログインからの上限」は操作を続けていても延長されません。最後にIDとパスワードを入力した日時(`last_login_at`)から3日が経過すると、その時点で強制的にログアウトし、`remember_token` も破棄します。

> Cookie の有効期限はブラウザ側で管理されるため改ざんされ得ます。実際の締め出しは毎リクエストで判定するサーバー側のミドルウェアが担っており、Cookie の期限はあくまで補助です。

### 無効化・削除の即時反映

- 無効化されたユーザー(`is_active = false`)は認証を通過できません(`EnsureUserIsActive` ミドルウェアが毎リクエストで確認)。
- 無効化・削除・パスワード再発行を行うと、対象ユーザーの **DBセッション行と `remember_token` をその場で破棄** します。ログイン中であっても即座に締め出されます。

## 3. 個人情報の暗号化と検索

- 氏名・カナ・電話・メール・住所・自由記述(対応内容/備考)は **アプリ層(Laravel Crypt / AES-256-GCM)で暗号化** して保存します。DB が漏えいしても `APP_KEY` なしでは復号できません。
- 完全一致検索のために **HMAC-SHA256 検索ハッシュ**(`phone_hash` / `email_hash`)を併存させます。鍵は `SEARCH_HASH_KEY`(`APP_KEY` とは別鍵)。
- 画面表示・部分検索用に電話番号 **下4桁のみ** 平文カラム(`phone_last4`)を保持します。
- `APP_KEY` / `SEARCH_HASH_KEY` はリポジトリに含めません。ローテーション時は再暗号化バッチが必要です。

## 4. 認可(ロールベース)

ロールは **管理者(`admin`) / 職員(`staff`) / メンバー(`member`)** の3種です。
管理者と職員は全画面にアクセスでき、メンバーは顧客管理・契約管理・ユーザー管理のみに限定されます。

| 画面・操作 | 管理者 | 職員 | メンバー |
| --- | :-: | :-: | :-: |
| ダッシュボード | ✔ | ✔ | — |
| 顧客管理 `/customers` (閲覧・検索) | ✔ | ✔ | ✔ |
| 顧客の登録 | ✔ | ✔ | ✔ |
| 顧客の編集 | 全件 | 担当のみ | 担当のみ |
| 顧客の削除・復元 | ✔ | — | — |
| 契約管理 `/contract` (閲覧・登録) | ✔ | ✔ | ✔ |
| 対応履歴 `/dialog` (閲覧・登録) | ✔ | ✔ | — |
| 請求・入金 `/payment` (閲覧・登録) | ✔ | ✔ | — |
| 操作履歴 | ✔ | ✔ | — |
| ユーザー管理を開く | ✔ | ✔ | ✔ |
| ユーザー管理で見える範囲 | 全ロール | 職員・メンバー | メンバーのみ |
| ユーザーの作成 | 全ロール | メンバーのみ | — |
| ユーザーの有効・無効切替 | ✔ | 職員・メンバー | — |

- 画面アクセスは Gate `access-dashboard` / `access-dialog` / `access-payment` / `view-audit-logs` / `access-users`、
  書込系は `write-data`、ユーザーの作成・有効無効は `manage-users` で保護します。
  ルートミドルウェア(`can:`)と Blade `@can` の二重で適用します。
- ユーザー管理の一覧は `UserRole::visibleRoles()`、作成可能なロールは `UserRole::creatableRoles()` が唯一の定義元です。
  権限昇格を防ぐため、作成時は FormRequest 側でも `Rule::in()` により検証します。
- 論理削除済み顧客の閲覧・復元は管理者のみ。
- メンバーはダッシュボードを開けないため、ログイン後は顧客一覧へ遷移します。

## 5. 監査ログ

- ログイン成否・登録/更新/削除/復元・ユーザー管理操作を `audit_logs` に記録(操作者・対象・IP・UA・日時)。
- `changed_fields` には **カラム名のみ** を保存し、変更前後の値(機密情報)は記録しません。
- 監査ログは画面からの編集・削除を提供しません。

## 6. 入力・出力対策

- 全更新系リクエストは FormRequest による検証+CSRF トークン必須。
- Blade は `{{ }}` によるエスケープ出力のみ使用({!! !!} 不使用)。
- 一覧・検索はプレースホルダ(Eloquent/クエリビルダ)経由で SQL インジェクションを防止。
- ID パラメータは `whereNumber` 制約+ポリシーで所有・権限確認。
- 電話番号は保存前に正規化(全角→半角、記号除去)し、検索ハッシュの一致性を担保。

## 7. フロントエンド

- Bootstrap 5.3.8 を **ローカル同梱**(CDN 不使用)。外部への通信は発生しません。
- 二重送信防止(送信済みフォームの再 submit 抑止)と危険操作の確認ダイアログを実装。
- `prefers-reduced-motion: reduce` で全アニメーション停止(視覚過敏への配慮)。

## 8. 運用上の注意

- 本番では HTTPS 終端(リバースプロキシ/ロードバランサ)を必ず前段に配置してください。
- `.env` ファイル・DB ボリュームのバックアップは暗号化ストレージに保管してください。
- 初期管理者作成は `php artisan app:create-admin` のみを使用し、SQL 直挿入は行わないでください。
